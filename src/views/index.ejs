<!DOCTYPE html>
<html lang="es">
<head>
    <%- include('partials/head') %>
    <link rel="stylesheet" href="/css/indexStyles.css">
</head>
<body>
    <%- include('partials/header') %>
    <h1 class="indexTitle">MTG Singles DFE</h1>
    <div class="services">
        <div class="servicesTitle">
            <h2>¡Material Sellado!</h2>
            <p>Accedé a los mejores precons de Commander para comenzar a jugar YA con tus amigos.</p>
        </div>
        <div class="servicesTitle">
            <h2>¡Vendemos singles!</h2>
            <p>Armá o completá tu deck con nuestro amplio stock de singles para todos los formatos.</p>
        </div>
    </div>
    <div class="buylistLink">
        <h4>Si quieres vender tus cartas <a href="/buylistForm">¡haz click aquí!</a></h4>
    </div>
    <main>
        <div class="plains">
            <div class="sectionContainer">
                <h3 class="indexTitle">Novedades</h3>
                <div class="container">
                    <% let newCount = 0 %>
                    <% for( let i = 0; i < cards.length; i++ ) { %>
                        <% if (cards[i].new == 1 && newCount < 3) { %>
                            <% newCount++ %>
                            <img class="img1" src="<%= cards[i].front_img %>"/>
                        <% } %>
                    <% } %>
                </div>
                <a class="linker" href="/new"><button class="indexButton">Ver mas</button></a>
            </div>
        </div>
        <div class="mountain">
            <div class="sectionContainer">
                <h3 class="indexTitle">Destacados</h3>
                <div class="container">
                    <% let featuredCount = 0 %>
                    <% for( let i = 0; i < cards.length; i++ ) { %>
                        <% if (cards[i].featured == 1 && featuredCount < 3) { %>
                            <% featuredCount++ %>
                            <img class="img1" src="<%= cards[i].front_img %>"/>
                        <% } %>
                    <% } %>
                </div>
                <a class="linker" href="/featured"><button class="indexButton">Ver mas</button></a>
            </div>
        </div>
        <div class="swamp">
            <div class="sectionContainer">
                <h3 class="indexTitle">Ofertas</h3>
                <div class="container">
                    <% let onSaleCount = 0 %>
                    <% for( let i = 0; i < cards.length; i++ ) { %>
                        <% if (cards[i].on_sale == 1 && onSaleCount < 3) { %>
                            <% onSaleCount++ %>
                            <img class="img1" src="<%= cards[i].front_img %>"/>
                        <% } %>
                    <% } %>
                </div>
                <a class="linker" href="/on_sale"><button class="indexButton">Ver mas</button></a>
            </div>
        </div>
    </main>
    <hr>
    <section>
        <div class="payment">
            <h4 class="indexTitle">Métodos de Pago</h4>
            <div class="paymentContainer">
                <div class="paymentIcon">
                    <i class="fa-brands fa-btc"></i>
                    <p>Bitcoin</p>
                </div>
                <div class="paymentIcon">
                    <i class="fa-solid fa-sack-dollar"></i>
                    <p>Efectivo</p>
                </div>
                <div class="paymentIcon">
                    <i class="fa-solid fa-building-columns"></i>
                    <p>Transferencia Bancaria</p>
                </div>
                <div class="paymentIcon">
                    <i class="fa-brands fa-cc-visa"></i>
                    <p>Tarjeta de Crédito</p>
                </div>
            </div>
        </div>
    </section>
    <%- include('partials/footer') %>
</body>
</html>